import java.util.*

fun main(){
    var listaCompras = arrayOf("Arroz", "Feijão", "Batata", "Uva", "Banana", "Carne")

    // acessar por indice
    println(listaCompras[2])

    // acessar array completo
    println(Arrays.toString(listaCompras))

    // tamanho da lista
    println(listaCompras.size)
}